name: lib_weather_screen
description: Custom drawer
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  http_request:
    path: ../../libs/http_request

  lib_location:
    path: ../../libs/lib_location

dev_dependencies:
  pedantic: ^1.9.0
